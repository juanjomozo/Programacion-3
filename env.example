# Variables de entorno para desarrollo local
# IMPORTANTE: Este archivo NO debe subirse a GitHub (debe estar en .gitignore)

# Configuración del servidor
PORT=3000
NODE_ENV=development
SECRET_KEY=mi_clave_secreta_super_segura_cambiar_en_produccion

# Base de datos PostgreSQL - DESARROLLO LOCAL
# Si usas PostgreSQL local, descomenta y configura:
# DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/restaurante_db

# Base de datos PostgreSQL - RENDER (Producción)
# Render te dará esta URL automáticamente, no necesitas configurarla aquí
# DATABASE_URL=postgresql://user:pass@host:5432/database
